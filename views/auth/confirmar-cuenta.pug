extends ../layout/index

block Contenido
    .py-10
        h1.text-center.text-4xl.font-extrabold Bienes
            span.font-normal Raices
        h2.text-center.text-2xl.font-extrabold.mt-10= pageTitle

        div.bg-white.py-10.px-5.border-slate-500.border-2.max-w-xl.mx-auto.mt-9
            p(class= error ? 'border-y-red-500' : 'border-y-green-500 ').text-xl.border-y-2.text-center.py-5.px-3.m-4= message
            if !error
                a.text-gray-500.text-sm.text-center.font-bold.uppercase.mt-12.block(href="/auth/login") Iniciar sesión