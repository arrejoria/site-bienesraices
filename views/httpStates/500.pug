extends ../layout/index

block Main_Content
    .flex.flex-col.justify-center.items-center.text-center.min-h-screen 
        h1.font-bold.text-5xl.mb-6.uppercase.text-gray-300= pageTitle
        div(class="bg-gray-100 p-9 rounded h-36 flex items-center")
            if message
                p.text-xl.text-gray-600=message
        
        a(href="/" class="mt-6 py-2 px-4 bg-sky-500 rounded text-white hover:bg-sky-600 transition-all ") Volver al inicio